/* global Darkmode */
const options = {
  bottom: '64px',
  time: '0.5s',
  mixColor: '#fff',
  backgroundColor: '#fff',
  buttonColorDark: '#100f2c',
  buttonColorLight: '#fff',
  label: '🌓'
}

const darkMode = new Darkmode(options)
darkMode.showWidget()
